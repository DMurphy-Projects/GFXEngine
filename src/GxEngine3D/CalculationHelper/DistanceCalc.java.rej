diff a/src/GxEngine3D/CalculationHelper/DistanceCalc.java b/src/GxEngine3D/CalculationHelper/DistanceCalc.java	(rejected hunks)
@@ -13,6 +13,16 @@ public class DistanceCalc {
 		return Math.sqrt(total);
 	}
 
+	public static double getDistanceFast(double[] d1, double[] d2)
+	{
+		double total = 0;
+		// assumes same dimensions
+		for (int i = 0; i < d1.length; i++) {
+			total += (d1[i] - d2[i]) * (d1[i] - d2[i]);
+		}
+		return FastMath.sqrt(total);
+	}
+
 	public static double getDistanceNoRoot(double[] d1, double[] d2) {
 		double total = 0;
 		// assumes same dimensions
